import {Fragment, ReactNode} from 'react';
import Layout from '@theme/Layout';

export default function Home(): ReactNode {
  return (
    <Layout description="Just another software engineer">
      <Fragment/>
    </Layout>
  );
}
